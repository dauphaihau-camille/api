{
  "description": "Default local smoke profile. Stays under the API default RATE_LIMIT_LIMIT=120/minute.",
  "vus": 1,
  "duration": "20s",
  "sleepSeconds": 1,
  "listLimit": 20,
  "thresholds": {
    "http_req_failed": ["rate<0.01"],
    "http_req_duration": ["p(95)<1000"],
    "checks": ["rate>0.98"]
  }
}
